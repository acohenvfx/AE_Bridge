# Nuxt 2 targets Node 16, but some transitive data packages (e.g. node-releases,
# pulled in by browserslist) declare engines >=18. They run fine on Node 16 —
# the constraint is over-strict — so don't let yarn abort the install on it.
ignore-engines true
